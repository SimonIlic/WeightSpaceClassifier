
## Installation
1. clone repo 
2. install uv
3. from WeightSpaceClassifier directory:
```bash
uv sync
uv pip install -e .
```

## Adding packages
Instead of `pip install <package_name>`:
```bash
uv add <package_name>
```
